run it:
	make
	./stack_test

========================
stack.h
Stack基类的定义，其中保存的是数据的void *类型指针


string_stack.h
StringStack类的定义(继承了Stack类)，其中保存数据是string*类型的指针，相对Stack来说，增加了类型检查，更加安全。
更好的实现应该是把stack当作成员，用组合的形式
=======================
stash.cpp
